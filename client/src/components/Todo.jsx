import styled from '@emotion/styled';
import { Card } from '@mui/material';

const StyledCard = styled(Card)`
  background-color: #eaeaea;
`;

const Todo = () => {
  return <StyledCard>This is a list</StyledCard>;
};

export default Todo;
